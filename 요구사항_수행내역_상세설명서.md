# 요구사항 수행내역 상세설명서

이 문서는 `요구사항_수행내역서.md`에 적힌 수행 결과를 초보자도 이해할 수 있도록 항목별로 풀어 쓴 설명서이다.

수행 내역서는 “무엇을 했고 어떤 결과가 나왔는지”를 증거 중심으로 정리한 문서이고, 이 상세설명서는 “그 명령과 출력이 무슨 뜻인지”를 설명하는 문서이다.

## 1. 수행 기준 상세 설명

이번 과제는 Docker 컨테이너를 하나의 Linux 실습 서버처럼 사용한다.

```text
컨테이너 이름: b1-1-agent-lab
서비스 이름: agent-lab
기반 OS: Ubuntu 24.04 LTS container
SSH 포트: 20022
APP 포트: 15034
AGENT_HOME: /home/agent-admin/agent-app
AGENT_LOG_DIR: /var/log/agent-app
```

각 항목의 의미는 다음과 같다.

- `컨테이너 이름`: 실제로 실행되는 Docker 컨테이너의 이름이다.
- `서비스 이름`: `docker-compose.yml`에서 정의한 서비스 이름이다.
- `기반 OS`: 컨테이너 안에서 사용하는 Linux 배포판이다.
- `SSH 포트`: SSH 접속을 받을 포트 번호이다.
- `APP 포트`: `agent-app`이 외부 요청을 받을 포트 번호이다.
- `AGENT_HOME`: 앱 파일, 스크립트, 업로드 디렉토리가 들어 있는 기준 경로이다.
- `AGENT_LOG_DIR`: 앱과 모니터링 로그를 저장하는 경로이다.

컨테이너 실행 명령은 다음과 같다.

```bash
docker compose up -d --build
docker compose exec agent-lab bash
```

명령어를 나누어 보면 다음과 같다.

- `docker`: Docker를 실행하는 명령이다.
- `compose`: 여러 컨테이너 설정을 `docker-compose.yml` 기준으로 실행한다.
- `up`: 서비스를 생성하고 실행한다.
- `-d`: detached 모드이다. 터미널을 계속 붙잡지 않고 백그라운드에서 실행한다.
- `--build`: 컨테이너를 실행하기 전에 이미지를 다시 빌드한다.
- `exec`: 이미 실행 중인 컨테이너 안에서 명령을 실행한다.
- `agent-lab`: 접속할 서비스 이름이다.
- `bash`: 컨테이너 안에서 Bash 쉘을 연다.

즉 첫 번째 명령은 실습 서버를 만들고 실행하는 명령이고, 두 번째 명령은 그 서버 안으로 들어가는 명령이다.

## 2. 컨테이너 구성 파일 상세 설명

수행 내역서에 적힌 구성 파일은 다음과 같다.

```text
Dockerfile
docker-compose.yml
container-init.sh
container-entrypoint.sh
agent-app
monitor.sh
```

각 파일의 목적은 다음과 같다.

- `Dockerfile`: 어떤 Linux 이미지를 사용할지, 어떤 패키지를 설치할지 정의한다.
- `docker-compose.yml`: 컨테이너 이름, 포트 연결, 권한, 실행 방식을 정의한다.
- `container-init.sh`: 계정, 그룹, 디렉토리, 권한, 환경 변수, cron 설정을 구성한다.
- `container-entrypoint.sh`: 컨테이너가 시작될 때 SSH, UFW, cron 같은 서비스를 실행한다.
- `agent-app`: 과제에서 실행해야 하는 앱 파일이다.
- `monitor.sh`: 앱 실행 상태와 포트, 자원 사용률을 확인하는 모니터링 스크립트이다.

파일 이름에 `.sh`가 붙은 것은 쉘 스크립트라는 뜻이다. 쉘 스크립트는 Linux 명령어를 순서대로 적어 자동 실행할 수 있게 만든 파일이다.

## 3. SSH 포트 변경 및 Root 원격 접속 차단 상세 설명

SSH는 원격으로 Linux 서버에 접속할 때 사용하는 서비스이다. 이번 실습에서는 컨테이너 내부 SSH 포트를 `20022`로 설정하고, `root` 계정의 직접 원격 접속을 막았다.

적용 설정은 다음과 같다.

```text
Port 20022
PermitRootLogin no
```

- `Port 20022`: SSH 서버가 `20022` 포트에서 접속을 기다리게 한다.
- `PermitRootLogin no`: `root` 계정으로 바로 SSH 로그인하는 것을 금지한다.

확인 명령은 다음과 같다.

```bash
sshd -T | grep -E '^(port|permitrootlogin)'
ss -tulnp | grep ':20022'
```

기호와 옵션의 의미는 다음과 같다.

- `sshd`: SSH 서버 프로그램이다.
- `-T`: SSH 서버의 최종 적용 설정을 출력한다. 설정 파일이 여러 개여도 실제 적용값을 볼 수 있다.
- `|`: 파이프이다. 왼쪽 명령의 출력 결과를 오른쪽 명령으로 넘긴다.
- `grep`: 특정 글자가 들어간 줄만 골라서 보여준다.
- `-E`: 확장 정규식을 사용한다.
- `^`: 줄의 시작을 의미한다.
- `(port|permitrootlogin)`: `port` 또는 `permitrootlogin` 중 하나를 의미한다.
- `ss`: 현재 열려 있는 네트워크 포트와 소켓 상태를 확인하는 명령이다.
- `-t`: TCP 연결만 보여준다.
- `-u`: UDP 연결도 보여준다.
- `-l`: LISTEN 상태, 즉 접속 대기 중인 포트를 보여준다.
- `-n`: 포트 번호를 이름으로 바꾸지 않고 숫자로 보여준다.
- `-p`: 해당 포트를 사용하는 프로세스 정보를 보여준다.
- `':20022'`: 출력 중에서 20022 포트가 포함된 줄만 찾는다.

정상 출력의 핵심은 다음과 같다.

```text
port 20022
permitrootlogin no
LISTEN ... :20022 ...
```

`LISTEN`은 서비스가 포트를 열고 접속을 기다리는 상태라는 뜻이다. `0.0.0.0:20022`는 모든 IPv4 주소에서 20022 포트 접속을 받을 수 있다는 뜻이고, `[::]:20022`는 IPv6 주소에서도 접속을 받을 수 있다는 뜻이다.

## 4. UFW 방화벽 설정 상세 설명

UFW(Uncomplicated Firewall(언컴플리케이티드 파이어월))는 Ubuntu에서 방화벽을 쉽게 설정하기 위한 도구이다. 방화벽은 들어오는 네트워크 요청 중 허용된 것만 통과시키는 역할을 한다.

이번 실습에서 허용한 포트는 다음과 같다.

```text
20022/tcp: SSH
15034/tcp: Agent APP
```

- `20022/tcp`: SSH 접속용 포트이다.
- `15034/tcp`: `agent-app` 접속용 포트이다.
- `/tcp`: TCP 방식의 네트워크 통신을 의미한다.

확인 명령은 다음과 같다.

```bash
ufw status
```

출력의 의미는 다음과 같다.

```text
Status: active
20022/tcp                  ALLOW       Anywhere
15034/tcp                  ALLOW       Anywhere
```

- `Status: active`: 방화벽이 켜져 있다는 뜻이다.
- `ALLOW`: 해당 포트 접속을 허용한다는 뜻이다.
- `Anywhere`: 어느 출발지에서든 접근을 허용한다는 뜻이다.
- `(v6)`: IPv6 네트워크에 대한 규칙이라는 뜻이다.

즉 수행 결과는 컨테이너 내부 방화벽이 켜져 있고, 필요한 두 포트만 허용되어 있음을 보여준다.

## 5. 계정 및 그룹 생성 확인 상세 설명

Linux에서는 사용자 계정과 그룹을 나누어 권한을 관리한다. 모든 작업을 `root`로 처리하면 위험하므로, 역할별 계정을 따로 만들었다.

생성 계정은 다음과 같다.

```text
agent-admin: 운영/관리, 앱 실행, cron 실행
agent-dev: monitor.sh 작성자
agent-test: 테스트 사용자
```

생성 그룹은 다음과 같다.

```text
agent-common: agent-admin, agent-dev, agent-test
agent-core: agent-admin, agent-dev
```

그룹을 나눈 목적은 다음과 같다.

- `agent-common`: 공용 업로드 디렉토리에 접근할 수 있는 일반 작업 그룹이다.
- `agent-core`: 키 파일과 로그처럼 더 중요한 자원에 접근할 수 있는 핵심 그룹이다.
- `agent-test`: 테스트 사용자이므로 `agent-core`에는 포함하지 않는다.

확인 명령은 다음과 같다.

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core
```

명령어 의미는 다음과 같다.

- `id 사용자명`: 해당 사용자의 UID, 기본 GID, 소속 그룹을 보여준다.
- `UID`: 사용자에게 부여된 숫자 ID이다.
- `GID`: 그룹에게 부여된 숫자 ID이다.
- `groups`: 사용자가 속한 그룹 목록이다.
- `getent group 그룹명`: 시스템에 등록된 그룹 정보를 조회한다.

예시 출력에서 확인할 핵심은 다음과 같다.

```text
agent-admin -> agent-common, agent-core 포함
agent-dev   -> agent-common, agent-core 포함
agent-test  -> agent-common만 포함
```

이 결과는 운영자와 개발자는 핵심 자원에 접근할 수 있고, 테스트 사용자는 공용 영역만 사용할 수 있게 분리되었음을 의미한다.

## 6. 디렉토리 구조 및 권한 확인 상세 설명

컨테이너 내부 서비스 경로는 다음과 같다.

```text
/home/agent-admin/agent-app
/home/agent-admin/agent-app/bin/monitor.sh
/home/agent-admin/agent-app/upload_files
/home/agent-admin/agent-app/api_keys/t_secret.key
/var/log/agent-app
```

각 경로의 목적은 다음과 같다.

- `/home/agent-admin/agent-app`: 앱의 기준 디렉토리이다.
- `bin/monitor.sh`: 실행 스크립트를 보관하는 위치이다.
- `upload_files`: 일반 사용자들이 함께 사용할 업로드 영역이다.
- `api_keys/t_secret.key`: 앱 실행에 필요한 키 파일이다.
- `/var/log/agent-app`: 앱과 모니터링 로그를 저장하는 위치이다.

확인 명령은 다음과 같다.

```bash
source /etc/profile.d/agent-app.sh
ls -ld "$AGENT_HOME" "$AGENT_HOME/bin" "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
ls -l "$AGENT_HOME/bin/monitor.sh" "$AGENT_HOME/api_keys/t_secret.key"
getfacl "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
```

기호와 옵션의 의미는 다음과 같다.

- `source`: 설정 파일을 현재 쉘에 불러온다.
- `/etc/profile.d/agent-app.sh`: `AGENT_HOME` 같은 환경 변수를 등록한 파일이다.
- `ls`: 파일과 디렉토리 목록을 보여준다.
- `-l`: 소유자, 그룹, 권한, 크기, 수정 시간까지 자세히 보여준다.
- `-d`: 디렉토리 안의 내용이 아니라 디렉토리 자체 정보를 보여준다.
- `$AGENT_HOME`: `AGENT_HOME` 환경 변수의 값을 사용한다.
- `"$AGENT_HOME"`: 경로에 공백이 있어도 하나의 값으로 안전하게 처리하기 위해 큰따옴표로 감싼다.
- `getfacl`: 기본 권한보다 더 자세한 ACL 권한 정보를 보여준다.

권한 문자열 예시는 다음과 같다.

```text
drwxrws---+
-rwxr-x---
-rw-rw----
```

앞 글자와 권한의 의미는 다음과 같다.

- `d`: 디렉토리이다.
- `-`: 일반 파일이다.
- `r`: 읽기 권한이다.
- `w`: 쓰기 권한이다.
- `x`: 실행 권한이다. 디렉토리에서는 안으로 들어갈 수 있는 권한이라는 뜻도 된다.
- `s`: setgid 권한이다. 디렉토리 안에 새 파일을 만들 때 그룹을 유지하는 데 사용된다.
- `+`: ACL 추가 권한이 설정되어 있다는 뜻이다.

권한은 보통 세 묶음으로 읽는다.

```text
rwx rwx ---
소유자 그룹 기타사용자
```

예를 들어 `drwxrws---+`는 디렉토리이고, 소유자와 그룹은 읽기/쓰기/실행이 가능하며, 기타 사용자는 접근할 수 없고, ACL이 추가로 설정되어 있다는 뜻이다.

ACL 출력의 주요 항목은 다음과 같다.

- `owner`: 파일 또는 디렉토리 소유자이다.
- `group`: 기본 소유 그룹이다.
- `user::rwx`: 소유자의 권한이다.
- `group::rwx`: 기본 그룹의 권한이다.
- `group:agent-core:rwx`: `agent-core` 그룹에 별도로 부여된 권한이다.
- `mask::rwx`: ACL에서 실제로 허용될 수 있는 최대 권한이다.
- `other::---`: 그 외 사용자는 권한이 없다는 뜻이다.
- `default:`: 새로 생성되는 하위 파일이나 디렉토리에 상속될 기본 권한이다.

이 항목은 업로드, 키, 로그 디렉토리가 역할별 그룹 정책에 맞게 분리되었는지를 확인하는 부분이다.

## 7. 환경 변수 및 키 파일 확인 상세 설명

환경 변수는 앱과 스크립트가 사용할 설정값이다. 경로를 코드에 매번 직접 쓰지 않고 변수 이름으로 불러올 수 있게 해준다.

등록된 환경 변수는 다음과 같다.

```bash
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key
export AGENT_LOG_DIR=/var/log/agent-app
```

기호와 명령어 의미는 다음과 같다.

- `export`: 현재 쉘뿐 아니라 그 안에서 실행되는 프로그램도 변수를 사용할 수 있게 내보낸다.
- `AGENT_HOME=...`: 변수에 값을 저장한다.
- `$AGENT_HOME`: 저장된 변수 값을 불러온다.
- `/`: Linux 경로 구분자이다.

확인 명령은 다음과 같다.

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && env | grep ^AGENT_'
sudo -u agent-admin bash -lc 'source ~/.bashrc && cat "$AGENT_KEY_PATH"'
```

기호와 옵션의 의미는 다음과 같다.

- `sudo -u agent-admin`: `agent-admin` 사용자 권한으로 명령을 실행한다.
- `bash -lc`: Bash 로그인 쉘을 열고 문자열로 전달한 명령을 실행한다.
- `'...'`: 작은따옴표 안의 내용을 하나의 명령 문자열로 전달한다.
- `~/.bashrc`: 사용자 쉘이 시작될 때 읽는 설정 파일이다.
- `&&`: 왼쪽 명령이 성공했을 때만 오른쪽 명령을 실행한다.
- `env`: 현재 환경 변수를 출력한다.
- `grep ^AGENT_`: `AGENT_`로 시작하는 줄만 보여준다.
- `cat`: 파일 내용을 화면에 출력한다.

키 파일 확인에서 다음 출력이 나오면 정상이다.

```text
agent_api_key_test
```

이 항목은 앱 실행에 필요한 경로와 키 파일이 `agent-admin` 사용자 기준으로 정상 조회되는지 확인하는 부분이다.

## 8. 앱 실행 및 Boot Sequence 확인 상세 설명

앱은 `root`가 아니라 일반 서비스 계정인 `agent-admin`으로 실행했다. 서비스 계정으로 실행하면 필요한 권한만 가지고 동작하므로 더 안전하다.

실행 명령은 다음과 같다.

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && cd "$AGENT_HOME" && ./agent-app'
```

명령어 의미는 다음과 같다.

- `sudo -u agent-admin`: `agent-admin` 계정으로 실행한다.
- `source ~/.bashrc`: 환경 변수를 불러온다.
- `cd "$AGENT_HOME"`: 앱 디렉토리로 이동한다.
- `./agent-app`: 현재 디렉토리에 있는 `agent-app` 파일을 실행한다.
- `./`: 현재 디렉토리를 의미한다. Linux에서는 현재 디렉토리의 실행 파일을 실행할 때 보통 `./파일명` 형태를 사용한다.

Boot Sequence는 앱이 시작되기 전에 필수 조건을 순서대로 점검하는 과정이다.

```text
[1/5] Checking User Account               [OK]
[2/5] Verifying Environment Variables     [OK]
[3/5] Checking Required Files             [OK]
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]
Agent READY
```

각 단계의 의미는 다음과 같다.

- `[1/5]`: 전체 5단계 중 1번째 단계라는 뜻이다.
- `[OK]`: 해당 점검이 성공했다는 뜻이다.
- `Checking User Account`: 올바른 사용자로 실행 중인지 확인한다.
- `Verifying Environment Variables`: 필요한 환경 변수가 있는지 확인한다.
- `Checking Required Files`: 키 파일 등 필수 파일이 있는지 확인한다.
- `Checking Port Availability`: 앱이 사용할 포트가 비어 있는지 확인한다.
- `Verifying Log Permission`: 로그 디렉토리에 쓸 수 있는지 확인한다.
- `Agent READY`: 앱이 정상 실행 준비를 마쳤다는 뜻이다.

포트 확인 명령은 다음과 같다.

```bash
ss -ltnp | grep ':15034'
```

여기서 `-ltnp`는 `-l -t -n -p`를 붙여 쓴 형태이다.

- `-l`: LISTEN 상태만 본다.
- `-t`: TCP 포트만 본다.
- `-n`: 숫자 포트 그대로 보여준다.
- `-p`: 프로세스 정보를 보여준다.

`0.0.0.0:15034`가 보이면 앱이 모든 IPv4 주소에서 15034 포트 접속을 받을 준비가 되었다는 뜻이다.

## 9. monitor.sh 실행 확인 상세 설명

`monitor.sh`는 앱 상태를 사람이 직접 여러 명령으로 확인하지 않아도 되도록 만든 점검 스크립트이다.

실행 명령은 다음과 같다.

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && "$AGENT_HOME/bin/monitor.sh"'
```

이 명령은 `agent-admin` 사용자로 환경 변수를 불러온 뒤 모니터링 스크립트를 실행한다.

확인 내용은 다음과 같다.

- `agent-app` 프로세스가 실행 중인지 확인한다.
- `15034` 포트가 LISTEN 상태인지 확인한다.
- UFW 방화벽이 활성화되어 있는지 확인한다.
- CPU, 메모리, 디스크 사용률을 수집한다.
- 기준치를 넘으면 `[WARNING]`을 출력한다.
- `/var/log/agent-app/monitor.log`에 결과를 한 줄로 기록한다.

출력에서 자주 보이는 표시의 의미는 다음과 같다.

- `[OK]`: 정상이다.
- `[WARNING]`: 실행은 가능하지만 주의가 필요하다.
- `[ERROR]`: 필수 조건이 실패했다.
- `PID`: 실행 중인 프로세스 번호이다.
- `CPU`: CPU 사용률이다.
- `MEM`: 메모리 사용률이다.
- `DISK_USED`: 디스크 사용률이다.

이 항목은 앱이 실행 중인지만 보는 것이 아니라, 운영 관점에서 계속 감시할 수 있는 상태인지 확인하는 부분이다.

## 10. 로그 누적 확인 상세 설명

로그는 프로그램이 언제 어떤 상태였는지 남기는 기록이다. 문제가 생겼을 때 원인을 찾는 데 중요하다.

확인 명령은 다음과 같다.

```bash
tail -n 5 /var/log/agent-app/monitor.log
```

명령어 의미는 다음과 같다.

- `tail`: 파일의 마지막 부분을 출력한다.
- `-n 5`: 마지막 5줄만 보여준다.
- `/var/log/agent-app/monitor.log`: 확인할 로그 파일 경로이다.

로그 형식은 다음과 같다.

```text
[YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK_USED:..%
```

각 값의 의미는 다음과 같다.

- `YYYY-MM-DD`: 연-월-일이다.
- `HH:MM:SS`: 시:분:초이다.
- `PID`: 감시 대상 앱의 프로세스 번호이다.
- `CPU`: 해당 시점의 CPU 사용률이다.
- `MEM`: 해당 시점의 메모리 사용률이다.
- `DISK_USED`: 디스크 사용률이다.
- `%`: 전체 대비 비율을 뜻한다.

수행 결과에서 시간이 다른 여러 줄이 보인다는 것은 `monitor.sh`가 한 번만 실행된 것이 아니라 계속 누적 기록을 남겼다는 뜻이다.

## 11. cron 매분 자동 실행 확인 상세 설명

cron은 Linux에서 정해진 시간마다 명령을 자동 실행해 주는 기능이다. 이번 실습에서는 `monitor.sh`를 매분 실행하도록 등록했다.

확인 명령은 다음과 같다.

```bash
crontab -u agent-admin -l
tail -n 5 /var/log/agent-app/monitor.log
sleep 70
tail -n 5 /var/log/agent-app/monitor.log
```

명령어 의미는 다음과 같다.

- `crontab`: cron 작업 목록을 관리한다.
- `-u agent-admin`: `agent-admin` 사용자의 cron을 대상으로 한다.
- `-l`: 등록된 cron 목록을 출력한다.
- `sleep 70`: 70초 동안 기다린다.

등록된 cron 내용은 다음과 같다.

```text
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor.cron.out 2>&1
```

앞의 별표 5개의 의미는 다음과 같다.

```text
분 시 일 월 요일
*  *  *  *  *
```

각 위치의 의미는 다음과 같다.

- 첫 번째 `*`: 매분
- 두 번째 `*`: 매시간
- 세 번째 `*`: 매일
- 네 번째 `*`: 매월
- 다섯 번째 `*`: 모든 요일

따라서 `* * * * *`는 “매분 실행”이라는 뜻이다.

뒤쪽 기호의 의미는 다음과 같다.

- `>>`: 명령의 일반 출력을 파일 끝에 추가한다. 기존 내용을 지우지 않는다.
- `/var/log/agent-app/monitor.cron.out`: cron 실행 출력이 저장되는 파일이다.
- `2>&1`: 오류 출력도 일반 출력과 같은 곳으로 보낸다.
- `2`: 오류 출력 번호이다.
- `1`: 일반 출력 번호이다.
- `&1`: 1번 출력이 가리키는 위치를 의미한다.

`sleep 70`을 사용하는 이유는 cron이 매분 실행되므로, 1분이 지난 뒤 로그가 새로 추가되는지 확인하기 위해서이다.

## 12. 제출 전 체크리스트 상세 설명

수행 내역서의 마지막 체크리스트는 과제 요구사항이 모두 검증되었는지 최종 확인하는 표이다.

```markdown
- [x] 컨테이너 `b1-1-agent-lab` 실행 확인
- [x] SSH 포트 `20022` 확인
- [x] Root 원격 접속 차단 확인
```

체크박스 기호의 의미는 다음과 같다.

- `-`: Markdown 목록 표시이다.
- `[ ]`: 아직 완료하지 않은 항목이다.
- `[x]`: 완료한 항목이다.
- `` `...` ``: 코드, 명령어, 파일명, 포트처럼 정확히 읽어야 하는 값을 표시할 때 사용한다.

체크리스트 항목별 목적은 다음과 같다.

- 컨테이너 실행 확인: 실습 서버가 정상 실행 중인지 확인한다.
- SSH 포트 확인: SSH가 요구사항 포트인 `20022`에서 열렸는지 확인한다.
- Root 원격 접속 차단 확인: 보안상 위험한 `root` 직접 로그인을 막았는지 확인한다.
- UFW 활성화 확인: 방화벽이 켜져 있고 필요한 포트만 허용되는지 확인한다.
- 계정 확인: 역할별 사용자가 만들어졌는지 확인한다.
- 그룹 확인: 사용자 권한 분리가 그룹 기준으로 맞는지 확인한다.
- 디렉토리 권한 및 ACL 확인: 업로드, 키, 로그 경로 접근 권한이 정책에 맞는지 확인한다.
- 환경 변수 확인: 앱과 스크립트가 필요한 경로와 포트를 찾을 수 있는지 확인한다.
- 키 파일 확인: 앱 실행에 필요한 비밀 키 파일이 존재하고 내용이 맞는지 확인한다.
- Boot Sequence 확인: 앱 실행 전 필수 조건 5단계가 모두 통과했는지 확인한다.
- `Agent READY` 확인: 앱이 정상적으로 준비 완료 상태가 되었는지 확인한다.
- 앱 포트 확인: 앱이 `15034` 포트에서 요청을 받을 수 있는지 확인한다.
- `monitor.sh` 확인: 모니터링 스크립트가 정상 동작하는지 확인한다.
- `monitor.log` 확인: 모니터링 결과가 로그 파일에 누적되는지 확인한다.
- cron 확인: 사람이 실행하지 않아도 매분 자동 점검되는지 확인한다.

## 13. 자주 나오는 기호 요약

수행 내역서에서 자주 나온 기호를 다시 정리하면 다음과 같다.

```text
|        왼쪽 명령의 출력을 오른쪽 명령으로 전달
&&       왼쪽 명령이 성공하면 오른쪽 명령 실행
$변수    환경 변수 값 사용
"..."    공백이 있는 값도 하나로 묶어서 처리
'...'    문자열을 그대로 전달
./       현재 디렉토리
^        줄의 시작
*        모든 값 또는 반복 조건
>>       파일 끝에 출력 추가
2>&1     오류 출력도 일반 출력과 같은 곳으로 보냄
[OK]     정상
[WARNING] 주의
[ERROR]  오류
```

이 기호들을 이해하면 수행 내역서에 적힌 명령이 단순히 복사해서 실행하는 문장이 아니라, 어떤 값을 확인하고 어떤 결과를 증명하는지 읽을 수 있다.
