# B1-1 상세설명서

이 문서는 컨테이너를 처음 사용하는 학습자도 B1-1 과제를 이해하고 따라갈 수 있도록 설명한다.

핵심은 간단하다.

```text
Docker 컨테이너 하나를 작은 Linux 서버처럼 사용한다.
그 안에 SSH, 방화벽, 계정, 권한, 앱, 모니터링, cron을 구성한다.
```

호스트 PC의 SSH나 방화벽을 바꾸는 실습이 아니다. 모든 명령은 컨테이너 안에서 실행한다.

## 1. 컨테이너가 이 과제의 실습 서버인 이유

요구사항에는 Ubuntu 22.04 또는 동등한 Linux 환경을 사용하고, 이전 미션에서 구성한 컨테이너/VM 환경 사용을 권장한다고 되어 있다.

그래서 이 과제에서는 Docker 컨테이너를 하나의 독립된 Linux 서버처럼 사용한다.

단, 제공된 `agent-app` 실행 파일은 `GLIBC_2.38` 이상을 요구한다. `ubuntu:22.04` 컨테이너에서는 앱이 실행되지 않으므로, 앱 실행까지 완료하기 위해 상위 LTS인 `ubuntu:24.04` 컨테이너를 사용한다.

컨테이너를 쓰면 좋은 점:

- 호스트 PC 설정을 망가뜨릴 위험이 적다.
- 계정과 그룹을 자유롭게 만들 수 있다.
- SSH 포트와 방화벽을 실습해도 컨테이너 안에만 적용된다.
- 실수하면 컨테이너를 지우고 다시 만들 수 있다.
- 같은 실습 환경을 반복해서 재현할 수 있다.

## 2. 호스트와 컨테이너 구분

초보자가 가장 헷갈리는 부분은 “내가 지금 어디에서 명령을 실행하는가”이다.

호스트:

```text
내 컴퓨터 또는 작업 루트가 있는 바깥 환경
docker compose 명령을 실행하는 곳
```

컨테이너:

```text
Docker로 만든 Ubuntu 24.04 실습 서버
SSH, UFW, 계정, 앱, cron을 확인하는 곳
```

작업 루트에서 컨테이너를 실행한다.

```bash
docker compose up -d --build
```

컨테이너 안으로 들어간다.

```bash
docker compose exec agent-lab bash
```

이후 대부분의 과제 확인 명령은 컨테이너 안에서 실행한다.

## 3. 포트 매핑 이해

컨테이너 안의 서비스는 기본적으로 컨테이너 안에만 있다. 호스트에서 접근하려면 포트를 연결해야 한다.

이번 과제의 포트 연결:

```text
호스트 localhost:20022 -> 컨테이너 SSH 20022
호스트 localhost:15034 -> 컨테이너 APP 15034
```

그래서 호스트에서 SSH를 시험할 때는 다음처럼 접속한다.

```bash
ssh -p 20022 agent-admin@localhost
```

이 명령은 호스트의 `20022` 포트로 접속하지만, Docker가 이 요청을 컨테이너 내부 `20022` 포트로 전달한다.

## 4. SSH 체크리스트 설명

SSH는 원격으로 Linux 서버에 접속하는 서비스다. 이번 실습에서는 컨테이너 내부 SSH 포트를 `20022`로 바꾼다.

확인 명령:

```bash
sshd -T | grep -E '^(port|permitrootlogin)'
ss -tulnp | grep ':20022'
```

정상 예시:

```text
port 20022
permitrootlogin no
LISTEN ... :20022 ...
```

의미:

- `port 20022`: SSH가 20022 포트를 사용한다.
- `permitrootlogin no`: root 계정으로 직접 SSH 접속할 수 없다.
- `LISTEN`: SSH 서비스가 접속을 기다리고 있다.

## 5. UFW 방화벽 체크리스트 설명

방화벽은 컨테이너 안으로 들어오는 네트워크 요청을 제한한다.

이번 과제에서는 두 포트만 허용한다.

```text
20022/tcp: SSH 접속
15034/tcp: Agent 앱 접속
```

확인 명령:

```bash
ufw status
```

정상 예시:

```text
Status: active
20022/tcp ALLOW
15034/tcp ALLOW
```

컨테이너에서 UFW를 쓰기 위해 `docker-compose.yml`에 `NET_ADMIN`, `NET_RAW` 권한을 추가했다. 이 권한은 컨테이너가 네트워크 규칙을 다룰 수 있게 해준다.

## 6. 계정/그룹 체크리스트 설명

실무에서는 모든 사람이 같은 계정으로 작업하지 않는다. 역할별로 계정을 나누고, 그룹으로 권한을 관리한다.

계정:

- `agent-admin`: 운영자, 앱 실행자, cron 실행자
- `agent-dev`: 스크립트 작성자
- `agent-test`: 테스트 담당자

그룹:

- `agent-common`: 세 계정 모두 포함
- `agent-core`: `agent-admin`, `agent-dev`만 포함

확인 명령:

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core
```

핵심은 `agent-test`가 `agent-core`에 들어가면 안 된다는 점이다. 테스트 계정은 키 파일과 운영 로그 같은 핵심 자원에 접근하지 못해야 한다.

## 7. 디렉토리 권한 체크리스트 설명

컨테이너 내부 앱 경로:

```text
/home/agent-admin/agent-app
```

하위 디렉토리:

```text
upload_files: 공용 업로드 영역
api_keys: 비밀 키 보관 영역
bin: 실행 스크립트 영역
```

로그 경로:

```text
/var/log/agent-app
```

권한 정책:

- `upload_files`: `agent-common` 그룹이 읽고 쓸 수 있다.
- `api_keys`: `agent-core` 그룹만 읽고 쓸 수 있다.
- `/var/log/agent-app`: `agent-core` 그룹만 읽고 쓸 수 있다.
- `monitor.sh`: `agent-dev`가 소유하고 `agent-core` 그룹이 실행할 수 있다.

확인 명령:

```bash
source /etc/profile.d/agent-app.sh
ls -ld "$AGENT_HOME" "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
ls -l "$AGENT_HOME/bin/monitor.sh"
getfacl "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
```

## 8. 환경 변수 체크리스트 설명

환경 변수는 앱과 스크립트가 필요한 경로를 알 수 있게 해주는 설정값이다.

필수 환경 변수:

```bash
AGENT_HOME=/home/agent-admin/agent-app
AGENT_PORT=15034
AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key
AGENT_LOG_DIR=/var/log/agent-app
```

확인 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && env | grep ^AGENT_'
```

환경 변수가 없거나 틀리면 `agent-app`의 Boot Sequence에서 실패한다.

## 9. 키 파일 체크리스트 설명

앱은 키 파일이 있는지, 내용이 맞는지 확인한다.

파일:

```text
/home/agent-admin/agent-app/api_keys/t_secret.key
```

내용:

```text
agent_api_key_test
```

확인 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && cat "$AGENT_KEY_PATH"'
```

이 파일은 민감한 파일이므로 `agent-core` 그룹만 접근할 수 있어야 한다.

## 10. 앱 실행 체크리스트 설명

앱은 root가 아니라 일반 계정 `agent-admin`으로 실행한다.

실행 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && cd "$AGENT_HOME" && ./agent-app'
```

정상 결과:

```text
[1/5] Checking User Account               [OK]
[2/5] Verifying Environment Variables     [OK]
[3/5] Checking Required Files             [OK]
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]
Agent READY
```

각 단계의 의미:

- 1단계: 실행 계정 확인
- 2단계: 환경 변수 확인
- 3단계: 키 파일 확인
- 4단계: 포트 사용 가능 여부 확인
- 5단계: 로그 디렉토리 쓰기 권한 확인

## 11. 포트 LISTEN 체크리스트 설명

앱이 정상 실행되면 `15034` 포트에서 요청을 기다린다.

확인 명령:

```bash
ss -ltnp | grep ':15034'
```

정상 예시:

```text
LISTEN ... 0.0.0.0:15034 ...
```

`LISTEN`은 서비스가 접속을 받을 준비가 되었다는 뜻이다.

## 12. monitor.sh 체크리스트 설명

`monitor.sh`는 Bash로 작성된 관제 스크립트다.

하는 일:

- `agent-app` 프로세스가 살아 있는지 확인
- `15034` 포트가 열려 있는지 확인
- UFW가 활성화되어 있는지 확인
- CPU, 메모리, 디스크 사용률 수집
- 임계값 초과 시 `[WARNING]` 출력
- `/var/log/agent-app/monitor.log`에 기록
- 로그 파일이 10MB를 넘으면 최대 10개까지 순환 보관

실행 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && "$AGENT_HOME/bin/monitor.sh"'
```

정상 결과에는 다음 항목이 보여야 한다.

```text
Checking process 'agent-app'... [OK]
Checking port 15034... [OK]
[RESOURCE MONITORING]
[INFO] Log appended: /var/log/agent-app/monitor.log
```

프로세스나 포트가 실패하면 스크립트는 `exit 1`로 종료한다. 자원 사용률이나 방화벽 문제는 경고만 출력한다.

## 13. 로그 체크리스트 설명

로그는 장애 분석을 위한 기록이다. `monitor.sh`가 실행될 때마다 한 줄씩 기록된다.

확인 명령:

```bash
tail -n 5 /var/log/agent-app/monitor.log
```

로그 형식:

```text
[2026-05-12 12:00:00] PID:1234 CPU:5.0% MEM:8.0% DISK_USED:12%
```

의미:

- `PID`: 앱 프로세스 번호
- `CPU`: CPU 사용률
- `MEM`: 메모리 사용률
- `DISK_USED`: 루트 파일시스템 사용률

## 14. cron 체크리스트 설명

cron은 정해진 시간마다 명령을 자동 실행하는 기능이다.

이번 과제에서는 `agent-admin` 계정의 crontab에 `monitor.sh`를 매분 실행하도록 등록한다.

확인 명령:

```bash
crontab -u agent-admin -l
```

정상 예시:

```text
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor.cron.out 2>&1
```

자동 실행 확인:

```bash
tail -n 5 /var/log/agent-app/monitor.log
sleep 70
tail -n 5 /var/log/agent-app/monitor.log
```

1분 뒤 로그 줄이 늘어나면 cron이 정상 동작한 것이다.

## 15. 과제 목표별 쉬운 설명

SSH 포트 변경과 Root 접속 차단:

SSH 기본 포트와 root 직접 접속은 공격자가 먼저 노리는 대상이다. 포트를 바꾸고 root 접속을 막으면 기본적인 위험을 줄일 수 있다.

필요 포트만 허용하는 방화벽:

서버에 필요한 입구만 열어두는 방식이다. 이번 과제에서는 SSH `20022`, 앱 `15034`만 허용한다.

역할 기반 계정/그룹과 ACL:

사람마다 필요한 권한만 주기 위한 방법이다. 테스트 계정은 업로드는 볼 수 있어도 키 파일은 볼 수 없어야 한다.

환경 변수:

앱과 스크립트가 같은 경로와 포트를 사용하도록 기준값을 정하는 방법이다.

관제 스크립트와 로그:

앱이 살아 있는지, 포트가 열렸는지, 자원이 부족하지 않은지 자동으로 확인하고 기록한다.

cron과 로그 보존:

사람이 직접 실행하지 않아도 매분 관제가 수행되게 한다. 로그가 너무 커지지 않도록 크기 기준으로 순환 보관한다.

## 16. 초보자용 전체 순서

1. 작업 루트에서 `docker compose up -d --build` 실행
2. `docker compose exec agent-lab bash`로 컨테이너 접속
3. SSH, UFW, 계정, 그룹 확인
4. 디렉토리 권한과 ACL 확인
5. 환경 변수와 키 파일 확인
6. `agent-admin`으로 `agent-app` 실행
7. 다른 터미널에서 컨테이너에 다시 접속
8. `monitor.sh` 실행
9. `monitor.log` 확인
10. cron 등록과 1분 후 로그 증가 확인
11. 결과를 `요구사항_수행내역서.md`에 기록
