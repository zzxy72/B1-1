# B1-1 컨테이너 활용 작업 계획서

## 1. 작업 기준

이 과제는 호스트 PC 설정을 수정하는 실습이 아니라, **Linux 컨테이너를 하나의 실습 서버로 사용해 서비스를 구축하고 관리하는 실습**으로 진행한다.

요구사항에서 확인한 근거:

```text
Ubuntu 22.04 LTS 또는 동등 리눅스 환경
이전 미션에서 구성한 Linux 실습 환경(컨테이너/VM)을 그대로 사용 권장
```

따라서 모든 설정과 확인은 컨테이너 내부에서 수행한다.

제공된 `agent-app` 바이너리는 `GLIBC_2.38` 이상을 요구한다. `ubuntu:22.04` 컨테이너에서는 앱이 실행되지 않으므로, 앱 실행까지 가능한 상위 LTS 환경인 `ubuntu:24.04` 컨테이너를 사용한다.

- SSH 설정: 컨테이너 내부 `sshd`
- 방화벽 설정: 컨테이너 내부 `ufw`
- 계정/그룹 생성: 컨테이너 내부 Linux 계정
- 앱 실행: 컨테이너 내부 `agent-admin` 계정
- cron 실행: 컨테이너 내부 `agent-admin` crontab
- 로그 기록: 컨테이너 내부 `/var/log/agent-app/monitor.log`

## 2. 작업 목표

컨테이너 안에 다음 조건을 만족하는 실습 서비스를 만든다.

```text
컨테이너 OS: Ubuntu 24.04 LTS
SSH 포트: 20022
APP 포트: 15034
관리 계정: agent-admin
개발 계정: agent-dev
테스트 계정: agent-test
공유 그룹: agent-common
핵심 그룹: agent-core
앱 경로: /home/agent-admin/agent-app
로그 경로: /var/log/agent-app
```

이 경로들은 과제 요구사항을 컨테이너 실습 서버 안에 구현한 값이다.

## 3. 새 파일 구성

작업 루트 구성:

```text
.
├── Dockerfile
├── docker-compose.yml
├── container-init.sh
├── container-entrypoint.sh
├── agent-app
├── monitor.sh
├── README.md
├── 요구사항/
│   └── b1-1.md
├── 요구사항_수행내역서.md
├── 상세설명서.md
└── 컨테이너_활용_작업계획서.md
```

각 파일의 역할:

- `Dockerfile`: Ubuntu 24.04 기반 실습 서버 이미지를 만든다.
- `docker-compose.yml`: 컨테이너 실행, 포트 연결, 네트워크 권한을 정의한다.
- `container-init.sh`: 컨테이너 이미지 생성 중 계정, 그룹, 디렉토리, 권한, SSH, cron을 구성한다.
- `container-entrypoint.sh`: 컨테이너 시작 시 SSH, cron, UFW를 실행한다.
- `agent-app`: 컨테이너 내부에서 실행할 제공 앱이다.
- `monitor.sh`: 컨테이너 내부에서 앱과 시스템 상태를 점검하는 Bash 스크립트다.

## 4. 컨테이너 내부 서비스 구조

컨테이너 안에 다음 구조를 만든다.

```text
/home/agent-admin/agent-app/
├── agent-app
├── bin/
│   └── monitor.sh
├── upload_files/
└── api_keys/
    └── t_secret.key

/var/log/agent-app/
└── monitor.log
```

권한 설계:

```text
/home/agent-admin/agent-app              agent-admin:agent-core   750
/home/agent-admin/agent-app/bin          agent-admin:agent-core   750
/home/agent-admin/agent-app/bin/monitor.sh agent-dev:agent-core   750
/home/agent-admin/agent-app/upload_files agent-admin:agent-common 2770
/home/agent-admin/agent-app/api_keys     agent-admin:agent-core   2770
/var/log/agent-app                       agent-admin:agent-core   2770
```

`2770`의 앞자리 `2`는 setgid 권한이다. 이 권한이 있으면 디렉토리 안에 새 파일을 만들 때 디렉토리의 그룹이 유지되어 협업 권한 관리가 쉬워진다.

## 5. 컨테이너 실행 계획

작업 루트에서 빌드 및 실행:

```bash
docker compose up -d --build
```

컨테이너 상태 확인:

```bash
docker compose ps
```

컨테이너 접속:

```bash
docker compose exec agent-lab bash
```

컨테이너 중지:

```bash
docker compose down
```

## 6. 컨테이너 포트 계획

컨테이너 내부 포트:

```text
20022/tcp: SSH
15034/tcp: agent-app
```

호스트에서 컨테이너로 연결되는 포트:

```text
localhost:20022 -> 컨테이너:20022
localhost:15034 -> 컨테이너:15034
```

SSH 접속 예시:

```bash
ssh -p 20022 agent-admin@localhost
```

초기 실습 비밀번호:

```text
agentpass
```

## 7. 컨테이너 내부 확인 계획

컨테이너 안에서 다음 순서로 확인한다.

1. SSH 설정 확인

```bash
sshd -T | grep -E '^(port|permitrootlogin)'
ss -tulnp | grep ':20022'
```

2. UFW 확인

```bash
ufw status
```

3. 계정/그룹 확인

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core
```

4. 디렉토리/ACL 확인

```bash
source /etc/profile.d/agent-app.sh
ls -ld "$AGENT_HOME" "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
getfacl "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
```

5. 앱 실행

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && cd "$AGENT_HOME" && ./agent-app'
```

6. 다른 터미널에서 monitor 실행

```bash
docker compose exec agent-lab bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && "$AGENT_HOME/bin/monitor.sh"'
```

7. 로그 확인

```bash
tail -n 5 /var/log/agent-app/monitor.log
```

8. cron 확인

```bash
crontab -u agent-admin -l
sleep 70
tail -n 5 /var/log/agent-app/monitor.log
```

## 8. 완료 기준

다음 결과가 확인되면 작업이 완료된 것으로 본다.

- 컨테이너가 Ubuntu 24.04 기반으로 실행된다.
- 컨테이너 내부 SSH 포트가 `20022`로 설정된다.
- 컨테이너 내부 Root SSH 접속이 차단된다.
- 컨테이너 내부 UFW가 `20022/tcp`, `15034/tcp`만 허용한다.
- 컨테이너 내부 계정/그룹이 요구사항과 일치한다.
- 컨테이너 내부 디렉토리 권한과 ACL이 요구사항과 일치한다.
- `agent-app` 실행 시 Boot Sequence 5단계가 `[OK]`이고 `Agent READY`가 출력된다.
- `agent-app`가 `0.0.0.0:15034`에서 LISTEN 상태다.
- `monitor.sh`가 프로세스/포트/자원 상태를 확인하고 로그를 남긴다.
- `agent-admin` crontab이 매분 `monitor.sh`를 실행한다.
