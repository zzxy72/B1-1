# 1. 도커 컴포즈 파일 설명 하라.

### 1. cap_add (Capabilities Add)

기본적으로 도커 컨테이너는 보안을 위해 호스트 시스템(내 컴퓨터)의 중요한 설정에 접근하지 못하도록 막혀 있습니다. `cap_add`는 그 **금지된 상자 중에서 필요한 열쇠만 골라서 컨테이너에게 주는 것**입니다.

* **`NET_ADMIN` (Network Administration):**
컨테이너가 자신의 네트워크 설정을 직접 바꿀 수 있게 해줍니다. 예를 들어, 방화벽(iptables)을 설정하거나, 새로운 네트워크 인터페이스를 추가하는 등의 작업을 할 때 꼭 필요합니다.
* **`NET_RAW` (Network Raw):**
네트워크의 '생 데이터(Raw packets)'를 직접 주고받을 수 있게 합니다. 주로 `ping` 명령어를 자유롭게 쓰거나, 네트워크 트래픽을 감시(패킷 스니핑)하는 도구를 돌릴 때 사용합니다.

> **쉽게 말하면:** "이 컨테이너는 네트워크 전문가니까, 컴퓨터의 네트워크 설정을 마음대로 만질 수 있는 권한을 줘!"라는 뜻입니다.

---

### 2. tty: true

`tty`는 **Terminal**의 약자입니다. 이 설정을 `true`로 하면 도커가 컨테이너 안에 가짜 터미널(가상 터미널)을 하나 만들어 줍니다.

* 우리가 리눅스 터미널 화면에서 글자를 입력하고 결과를 보는 그 환경을 제공하는 것입니다.
* 이 설정이 없으면, 컨테이너가 실행되자마자 "사용자와 대화할 창구가 없네?"라고 판단하고 바로 종료되어 버리는 경우가 많습니다.

---

### 3. stdin_open: true

`stdin`은 Standard Input(표준 입력)의 약자입니다.

* `stdin_open: true`는 "컨테이너로 들어가는 입력 통로를 계속 열어둬라"라는 명령입니다.
* 보통 `tty: true`와 짝꿍으로 사용됩니다. `tty`가 **화면**을 만들어준다면, `stdin_open`은 **키보드 입력**을 가능하게 해준다고 보시면 됩니다.

---

### 💡 요약하자면?

이 설정들이 모두 들어간 `agent-lab` 서비스는 아마도 다음과 같은 용도일 확률이 높습니다.

1. **네트워크 실험 도구:** 네트워크 패킷을 분석하거나 방화벽 설정을 테스트해야 하는 경우.
2. **대화형 터미널 사용:** `docker exec -it ...` 명령어를 통해 직접 컨테이너 안으로 들어가서 명령어를 입력하고, 그 결과를 실시간으로 확인하며 작업해야 하는 환경.

즉, "네트워크 권한을 빵빵하게 가진 리눅스 터미널 서버를 하나 띄우겠다"는 설정이라고 이해하시면 완벽합니다!

----------------------------------------------------

# 2. 도커 파일 내용을 설명 하라.

주요 패키지(SSH, 방화벽, 크론 등)를 설치하고, 특정 애플리케이션(`agent-app`)을 가동하기 위한 준비 과정을 작성함.

---

### 1. 베이스 이미지 및 환경 설정

* **`FROM ubuntu:24.04`**: 최신 우분투 LTS 버전을 기본 운영체제로 사용합니다.
* **`ENV DEBIAN_FRONTEND=noninteractive`**: 패키지 설치 중 사용자에게 "예/아니오"를 묻는 프롬프트가 뜨지 않게 설정합니다. (자동 빌드를 위해 필수적입니다.)

---

### 2. 필수 패키지 설치 (`apt-get install`)

컨테이너 내에서 서버처럼 동작하기 위해 필요한 도구들을 설치합니다.

* **`acl`**: 정교한 파일 권한 관리 도구.
* **`cron`**: 예약된 시간에 작업을 실행하는 스케줄러.
* **`iproute2`**: `ip` 명령어 등 네트워크 설정 도구.
* **`openssh-server`**: 원격 접속(SSH)을 가능하게 합니다.
* **`procps`**: `ps`, `top` 등 프로세스 관리 도구.
* **`sudo`**: 일반 사용자에게 관리자 권한을 부여.
* **`ufw`**: 우분투 기본 방화벽. (앞서 본 `NET_ADMIN` 권한이 필요한 이유입니다.)

---

### 3. 파일 복사 및 권한 부여 (`COPY`, `chmod`)

호스트(컴퓨터)에 있는 파일들을 컨테이너 내부로 옮기고 실행 권한을 줍니다.

* **`agent-app`**: 이 컨테이너의 핵심 애플리케이션으로 보입니다.
* **`monitor.sh`**: 시스템이나 앱의 상태를 감시하는 스크립트입니다.
* **`container-init.sh`**: 빌드 시점에 **초기 설정**을 수행하는 스크립트입니다.
* **`container-entrypoint.sh`**: 컨테이너가 **시작될 때마다** 실행될 메인 스크립트입니다.

---

### 4. 빌드 타임 초기화 로직

```dockerfile
RUN chmod 755 ... \
  && /usr/local/bin/container-init.sh \
  && rm -rf /tmp/agent-build

```

* 준비한 스크립트들에 실행 권한(`755`)을 줍니다.
* **`/usr/local/bin/container-init.sh` 실행**: 이 단계에서 유저 생성, SSH 키 설정, 앱 설치 등의 무거운 작업이 한 번 수행됩니다.
* **`rm -rf /tmp/agent-build`**: 빌드가 끝나면 용량을 줄이기 위해 임시 폴더를 삭제합니다. (도커 이미지 최적화 기법입니다.)

---

### 5. 포트 개방 및 실행 (`EXPOSE`, `ENTRYPOINT`)

* **`EXPOSE 20022 15034`**: 컨테이너가 20022번(아마도 커스텀 SSH용)과 15034번 포트를 사용할 것임을 명시합니다.
* **`ENTRYPOINT [".../container-entrypoint.sh"]`**: 컨테이너가 구동될 때 이 스크립트를 가장 먼저 실행합니다. 보통 이 안에서 `sshd` 서비스나 `cron`, `agent-app`을 백그라운드로 띄우는 로직이 들어있을 것입니다.

---

### 💡 이 도커파일의 특징 (한 줄 요약)

단순히 프로그램 하나를 돌리는 용도가 아니라, "방화벽과 SSH 접속이 가능하고 스스로 상태를 감시하는 독립된 보안 가상 서버(Agent)"를 만들기 위한 설계도입니다.

이 도커파일 내용 중 `container-init.sh` 내부 로직이 궁금하시다면, 그 파일의 내용도 함께 확인해보시는 것이 좋습니다! 추가로 궁금한 점이 있으신가요?

-------

# 3. sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config ------ 상세 설명 ?
이 명령어는 리눅스 서버 보안 설정에서 가장 기본적이면서도 중요한 작업 중 하나인 "Root 계정의 원격 접속 차단"을 수행하는 명령입니다.

`sed`라는 강력한 텍스트 편집 도구를 사용하여 `/etc/ssh/sshd_config` 파일의 내용을 직접 수정하는 로직입니다. 각 부분을 쪼개서 상세히 설명해 드릴게요.

---

### 1. 명령어 기본 구조

* **`sed`**: **S**tream **ED**itor의 약자로, 파일을 열지 않고도 텍스트를 검색, 치환, 삭제할 수 있는 편집기입니다.
* **`-i`**: **i**n-place의 약자입니다. 변경 내용을 화면에 출력만 하는 게 아니라, **실제 파일에 즉시 저장**하라는 뜻입니다.
* **`'s/찾을내용/바꿀내용/'`**: **s**ubstitute(치환) 명령입니다. '찾을 내용'을 '바꿀 내용'으로 바꾸라는 뜻입니다.

---

### 2. 정규표현식 분석 (찾을 내용: `^#\?PermitRootLogin .*`)

이 부분이 가장 핵심이며, 다양한 상황에 대비하도록 설계되어 있습니다.

* **`^`**: 줄의 시작을 의미합니다.
* **`#\?`**:
* `#`은 주석 표시입니다.
* `\?`는 "앞의 글자(#)가 **있을 수도 있고 없을 수도 있다**"는 뜻입니다. (주석 처리가 되어 있든 아니든 다 찾아내기 위함입니다.)


* **`PermitRootLogin`**: 찾으려는 핵심 키워드입니다.
* **` .*`**: 공백 한 칸 뒤에 오는 모든 문자(`.`)를 개수 상관없이(`*`) 끝까지 다 포함하라는 뜻입니다. (기존에 `yes`라고 되어 있든 `prohibit-password`라고 되어 있든 상관없이 통째로 잡습니다.)

---

### 3. 바꿀 내용 (`PermitRootLogin no`)

* 앞에서 찾아낸 줄 전체를 `PermitRootLogin no`로 덮어씌웁니다.
* 결과적으로 주석(`#`)은 제거되고, 값은 `no`로 강제 고정됩니다.

---

### 4. 대상 파일 (`/etc/ssh/sshd_config`)

* SSH 서비스의 환경 설정 파일입니다. 이 파일에서 설정을 변경해야만 원격 접속 규칙이 바뀝니다.

---

### 💡 이 명령을 실행하면 어떻게 변하나요?

파일 안에 다음과 같이 되어 있던 내용들이...

* `#PermitRootLogin prohibit-password` (주석 처리된 경우)
* `PermitRootLogin yes` (허용되어 있는 경우)

**명령어 실행 후에는 모두 아래와 같이 일관되게 바뀝니다.**

> **`PermitRootLogin no`**

---

### ⚠️ 주의사항

1. **반드시 재시작 필요**: 이 명령으로 파일 내용만 바꾼다고 바로 적용되지는 않습니다. 시스템에 적용하려면 SSH 서비스를 재시작해야 합니다.
* `systemctl restart ssh` 또는 `service ssh restart`


2. **로그인 수단 확보**: Root 접속을 막기 전에, 반드시 `sudo` 권한을 가진 **일반 사용자 계정**이 있는지 확인하세요. 그렇지 않으면 나중에 서버에 원격으로 들어갈 방법이 없어질 수 있습니다.

**요약하자면:** 이 코드는 "주석 여부나 기존 설정값에 상관없이 **Root 사용자가 SSH로 로그인하는 것을 확실하게 금지**하도록 파일을 수정해라!"라는 매우 효율적인 관리자용 명령어입니다.

------------


# 4. 과제목표 "쉘 스크립트로 프로세스/포트/리소스 상태를 수집하고, 로그로 남겨 운영 문제를 추적하는 흐름을 설명할 수 있다." 는 어디에서 확인하나요?

이 과제목표는 `monitor.sh_상세설명서.md`에서 확인할 수 있습니다.

특히 아래 부분이 직접 연결됩니다.

* **4. 전체 동작 순서**
  * `monitor.sh`가 어떤 순서로 점검하는지 전체 흐름을 보여줍니다.
  * 로그 디렉토리 확인 → 로그 순환 보관 → 프로세스 확인 → 포트 확인 → 리소스 확인 → 로그 기록 순서입니다.

* **9. agent-app 프로세스 확인**
  * `agent-app`이 실제로 실행 중인지 확인하는 부분입니다.
  * 실행 중인 프로세스가 없으면 서비스가 죽은 상태일 수 있으므로 `[ERROR]`를 내고 종료합니다.

* **10. 15034 포트 확인**
  * 앱이 `15034` 포트에서 접속을 기다리는지 확인하는 부분입니다.
  * 프로세스가 살아 있어도 포트가 열려 있지 않으면 외부 요청을 받을 수 없기 때문에 중요한 점검입니다.

* **12. CPU 사용률 계산**
  * CPU가 얼마나 바쁜지 확인하는 부분입니다.

* **13. 메모리 사용률 계산**
  * 메모리를 얼마나 사용 중인지 확인하는 부분입니다.

* **14. 디스크 사용률 확인**
  * 디스크 공간이 얼마나 찼는지 확인하는 부분입니다.

* **15. 임계값 경고**
  * CPU, 메모리, 디스크 사용률이 기준값을 넘으면 `[WARNING]`을 출력하는 부분입니다.
  * 경고는 서비스가 바로 죽었다는 뜻은 아니지만, 운영자가 미리 확인해야 하는 신호입니다.

* **16. 로그 기록**
  * 점검 결과를 `/var/log/agent-app/monitor.log`에 한 줄로 남기는 부분입니다.
  * 이 부분이 "로그로 남겨 운영 문제를 추적한다"는 과제목표와 가장 직접적으로 연결됩니다.

---

## 초보자도 이해하기 쉬운 설명

이 과제목표는 쉽게 말해, "서버가 지금 잘 살아 있는지 자동으로 확인하고, 나중에 문제가 생겼을 때 원인을 찾을 수 있도록 기록을 남긴다"는 뜻입니다.

서버 운영에서는 앱이 제대로 동작하는지 눈으로 계속 볼 수 없습니다. 그래서 `monitor.sh` 같은 쉘 스크립트를 만들어서 사람이 하던 확인 작업을 자동화합니다.

스크립트가 확인하는 내용은 크게 세 가지입니다.

1. **프로세스 확인**

프로세스는 실행 중인 프로그램입니다.

이 과제에서는 `agent-app`이 실행 중인지 확인합니다.

예를 들어 앱이 꺼져 있으면 사용자는 서비스를 이용할 수 없습니다. 그래서 스크립트는 먼저 "앱 프로그램이 살아 있나?"를 확인합니다.

2. **포트 확인**

포트는 외부 요청이 들어오는 문입니다.

`agent-app`은 `15034` 포트에서 요청을 받아야 합니다. 앱 프로세스가 실행 중이어도 `15034` 포트가 열려 있지 않으면 외부에서 접속할 수 없습니다.

그래서 스크립트는 "앱이 15034번 문을 열고 기다리고 있나?"를 확인합니다.

3. **리소스 확인**

리소스는 서버가 사용하는 자원입니다.

대표적으로 CPU, 메모리, 디스크가 있습니다.

* CPU 사용률이 너무 높으면 서버가 너무 바쁘다는 뜻입니다.
* 메모리 사용률이 너무 높으면 프로그램이 느려지거나 죽을 수 있습니다.
* 디스크 사용률이 너무 높으면 로그나 파일을 더 이상 저장하지 못할 수 있습니다.

그래서 스크립트는 CPU, 메모리, 디스크 사용률을 확인하고, 기준값을 넘으면 `[WARNING]`을 출력합니다.

---

## 왜 로그로 남겨야 하나요?

로그는 서버의 상태 기록입니다.

예를 들어 어느 날 서비스가 느려졌다고 가정해 보겠습니다. 로그가 없으면 "언제부터 느려졌는지", "CPU가 높았는지", "메모리가 부족했는지", "디스크가 꽉 찼는지"를 추측해야 합니다.

하지만 `monitor.log`에 아래처럼 기록이 남아 있으면:

```text
[2026-05-12 16:30:00] PID:1234 CPU:25.3% MEM:8.1% DISK_USED:12%
```

운영자는 이렇게 판단할 수 있습니다.

* `PID:1234`: 이 시간에 앱 프로세스가 실행 중이었다.
* `CPU:25.3%`: CPU 사용률이 기준보다 높았다.
* `MEM:8.1%`: 메모리는 아직 괜찮았다.
* `DISK_USED:12%`: 디스크도 아직 괜찮았다.

즉, 로그를 보면 문제 원인을 감으로 맞히는 것이 아니라 기록을 보고 추적할 수 있습니다.

---

## 한 문장으로 정리

`monitor.sh`는 `agent-app` 프로세스가 살아 있는지, `15034` 포트가 열려 있는지, CPU/메모리/디스크가 위험하지 않은지 확인하고, 그 결과를 `monitor.log`에 남겨 나중에 장애 원인을 추적할 수 있게 해주는 운영 점검 스크립트입니다.


