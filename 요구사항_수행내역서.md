# B1-1 요구사항 수행 내역서

## 1. 수행 기준

본 과제는 Docker 컨테이너를 Linux 실습 서버로 사용하여 수행한다.

요구사항의 개발 환경에는 “Ubuntu 22.04 LTS 또는 동등 리눅스 환경” 및 “이전 미션에서 구성한 Linux 실습 환경(컨테이너/VM)을 그대로 사용 권장”이라고 되어 있으므로, 모든 설정과 검증은 컨테이너 내부에서 진행한다.

제공된 `agent-app` 바이너리는 `GLIBC_2.38` 이상을 요구하여 `ubuntu:22.04`에서는 실행되지 않는다. 앱 실행까지 검증하기 위해 상위 LTS 환경인 `ubuntu:24.04` 컨테이너를 사용한다.

실습 환경:

```text
컨테이너 이름: b1-1-agent-lab
서비스 이름: agent-lab
기반 OS: Ubuntu 24.04 LTS container
SSH 포트: 20022
APP 포트: 15034
AGENT_HOME: /home/agent-admin/agent-app
AGENT_LOG_DIR: /var/log/agent-app
```

컨테이너 실행:

```bash
docker compose up -d --build
docker compose exec agent-lab bash
```

## 2. 컨테이너 구성 파일

사용 파일:

```text
Dockerfile
docker-compose.yml
container-init.sh
container-entrypoint.sh
agent-app
monitor.sh
```

구성 방식:

- `Dockerfile`: Ubuntu 24.04 이미지에 SSH, UFW, cron, ACL 도구를 설치한다.
- `container-init.sh`: 계정, 그룹, 디렉토리, 권한, 환경 변수, SSH 설정, cron을 구성한다.
- `container-entrypoint.sh`: 컨테이너 시작 시 UFW, SSH, cron을 실행한다.
- `monitor.sh`: 컨테이너 내부의 앱 프로세스, 포트, 자원 상태를 점검하고 로그를 남긴다.

## 3. SSH 포트 변경 및 Root 원격 접속 차단

컨테이너 내부 `/etc/ssh/sshd_config`에 다음 설정을 적용했다.

```text
Port 20022
PermitRootLogin no
```

확인 명령:

```bash
sshd -T | grep -E '^(port|permitrootlogin)'
ss -tulnp | grep ':20022'
```

증거 기록:

```text
[붙여넣기] port 20022 확인 결과
[붙여넣기] permitrootlogin no 확인 결과
[붙여넣기] 컨테이너 내부 sshd 20022 LISTEN 확인 결과

root@agent-lab:/# sshd -T | grep -E '^(port|permitrootlogin)'
port 20022
permitrootlogin no
root@agent-lab:/# 
root@agent-lab:/# 
root@agent-lab:/# ss -tulnp | grep ':20022'
tcp   LISTEN 0      128          0.0.0.0:20022      0.0.0.0:*    users:(("sshd",pid=183,fd=3))
tcp   LISTEN 0      128             [::]:20022         [::]:*    users:(("sshd",pid=183,fd=4))

```

호스트에서 SSH 접속 확인:

```bash
ssh -p 20022 agent-admin@localhost
```

## 4. 컨테이너 내부 UFW 방화벽 설정

컨테이너 내부에서 UFW를 사용해 인바운드 허용 포트를 제한한다.

허용 포트:

```text
20022/tcp: SSH
15034/tcp: Agent APP
```

확인 명령:

```bash
ufw status
```

증거 기록:

```text
[붙여넣기] UFW active 확인 결과
[붙여넣기] 20022/tcp ALLOW 확인 결과
[붙여넣기] 15034/tcp ALLOW 확인 결과

root@agent-lab:/# ufw status
Status: active

To                         Action      From
--                         ------      ----
20022/tcp                  ALLOW       Anywhere                  
15034/tcp                  ALLOW       Anywhere                  
20022/tcp (v6)             ALLOW       Anywhere (v6)             
15034/tcp (v6)             ALLOW       Anywhere (v6)     

```

## 5. 계정/그룹 생성 확인

생성 계정:

```text
agent-admin: 운영/관리, 앱 실행, cron 실행
agent-dev: monitor.sh 작성자
agent-test: 테스트 사용자
```

생성 그룹:

```text
agent-common: agent-admin, agent-dev, agent-test
agent-core: agent-admin, agent-dev
```

확인 명령:

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core

```

증거 기록:

```text
[붙여넣기] id agent-admin 결과
[붙여넣기] id agent-dev 결과
[붙여넣기] id agent-test 결과
[붙여넣기] agent-common 그룹 결과
[붙여넣기] agent-core 그룹 결과


root@agent-lab:/# 
root@agent-lab:/# id agent-admin
uid=1001(agent-admin) gid=1003(agent-admin) groups=1003(agent-admin),1001(agent-common),1002(agent-core)
root@agent-lab:/# id agent-dev
uid=1002(agent-dev) gid=1004(agent-dev) groups=1004(agent-dev),1001(agent-common),1002(agent-core)
root@agent-lab:/# id agent-test
uid=1003(agent-test) gid=1005(agent-test) groups=1005(agent-test),1001(agent-common)
root@agent-lab:/# getent group agent-common
agent-common:x:1001:agent-admin,agent-dev,agent-test
root@agent-lab:/# getent group agent-core
agent-core:x:1002:agent-admin,agent-dev
root@agent-lab:/# 


```

## 6. 디렉토리 구조 및 권한 확인

컨테이너 내부 서비스 경로:

```text
/home/agent-admin/agent-app
/home/agent-admin/agent-app/bin/monitor.sh
/home/agent-admin/agent-app/upload_files
/home/agent-admin/agent-app/api_keys/t_secret.key
/var/log/agent-app
```

권한 정책:

```text
upload_files: agent-common 그룹 읽기/쓰기 가능
api_keys: agent-core 그룹만 읽기/쓰기 가능
/var/log/agent-app: agent-core 그룹만 읽기/쓰기 가능
monitor.sh: agent-dev:agent-core, 750
```

확인 명령:

```bash
source /etc/profile.d/agent-app.sh
ls -ld "$AGENT_HOME" "$AGENT_HOME/bin" "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
ls -l "$AGENT_HOME/bin/monitor.sh" "$AGENT_HOME/api_keys/t_secret.key"
getfacl "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
```

증거 기록:

```text
[붙여넣기] 디렉토리 소유자/그룹/권한 확인 결과
[붙여넣기] monitor.sh 750 권한 확인 결과
[붙여넣기] ACL 확인 결과

root@agent-lab:/# source /etc/profile.d/agent-app.sh
root@agent-lab:/# ls -ld "$AGENT_HOME" "$AGENT_HOME/bin" "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
drwxr-x---  1 agent-admin agent-core   64 May 12 06:53 /home/agent-admin/agent-app
drwxrws---+ 1 agent-admin agent-core   24 May 12 06:53 /home/agent-admin/agent-app/api_keys
drwxr-x---  1 agent-admin agent-core   20 May 12 06:53 /home/agent-admin/agent-app/bin
drwxrws---+ 1 agent-admin agent-common  0 May 12 06:53 /home/agent-admin/agent-app/upload_files
drwxrws---+ 1 agent-admin agent-core   32 May 12 06:54 /var/log/agent-app
root@agent-lab:/# 
root@agent-lab:/# ls -l "$AGENT_HOME/bin/monitor.sh" "$AGENT_HOME/api_keys/t_secret.key"
-rw-rw---- 1 agent-admin agent-core   19 May 12 06:53 /home/agent-admin/agent-app/api_keys/t_secret.key
-rwxr-x--- 1 agent-dev   agent-core 5394 May 12 06:53 /home/agent-admin/agent-app/bin/monitor.sh
root@agent-lab:/# 
root@agent-lab:/# 
root@agent-lab:/# getfacl "$AGENT_HOME/upload_files" "$AGENT_HOME/api_keys" /var/log/agent-app
getfacl: Removing leading '/' from absolute path names
# file: home/agent-admin/agent-app/upload_files
# owner: agent-admin
# group: agent-common
# flags: -s-
user::rwx
group::rwx
group:agent-common:rwx
mask::rwx
other::---
default:user::rwx
default:group::rwx
default:group:agent-common:rwx
default:mask::rwx
default:other::---

# file: home/agent-admin/agent-app/api_keys
# owner: agent-admin
# group: agent-core
# flags: -s-
user::rwx
group::rwx
group:agent-core:rwx
mask::rwx
other::---
default:user::rwx
default:group::rwx
default:group:agent-core:rwx
default:mask::rwx
default:other::---

# file: var/log/agent-app
# owner: agent-admin
# group: agent-core
# flags: -s-
user::rwx
group::rwx
group:agent-core:rwx
mask::rwx
other::---
default:user::rwx
default:group::rwx
default:group:agent-core:rwx
default:mask::rwx
default:other::---


```

## 7. 환경 변수 및 키 파일 확인

컨테이너 내부 `/etc/profile.d/agent-app.sh`에 다음 환경 변수를 등록했다.

```bash
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key
export AGENT_LOG_DIR=/var/log/agent-app
```

키 파일:

```text
/home/agent-admin/agent-app/api_keys/t_secret.key
내용: agent_api_key_test
```

확인 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && env | grep ^AGENT_'
sudo -u agent-admin bash -lc 'source ~/.bashrc && cat "$AGENT_KEY_PATH"'
```

증거 기록:

```text
[붙여넣기] AGENT_* 환경 변수 확인 결과
root@agent-lab:/# sudo -u agent-admin bash -lc 'source ~/.bashrc && env | grep ^AGENT_'
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files
AGENT_PORT=15034
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key
AGENT_HOME=/home/agent-admin/agent-app
AGENT_LOG_DIR=/var/log/agent-app

[붙여넣기] 키 파일 내용 확인 결과
root@agent-lab:/# sudo -u agent-admin bash -lc 'source ~/.bashrc && cat "$AGENT_KEY_PATH"'
agent_api_key_test
root@agent-lab:/# 

```

## 8. 앱 실행 및 Boot Sequence 확인

앱은 컨테이너 내부 일반 계정 `agent-admin`으로 실행한다.

실행 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && cd "$AGENT_HOME" && ./agent-app'
```

정상 기준:

```text
[1/5] Checking User Account               [OK]
[2/5] Verifying Environment Variables     [OK]
[3/5] Checking Required Files             [OK]
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]
Agent READY
```

포트 확인 명령:

```bash
ss -ltnp | grep ':15034'
```

증거 기록:

```text
[붙여넣기] Boot Sequence 5단계 [OK] 출력
[붙여넣기] Agent READY 출력

root@agent-lab:/# sudo -u agent-admin bash -lc 'source ~/.bashrc && cd "$AGENT_HOME" && ./agent-app'
>>> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
 ... Running as service user 'agent-admin' (uid=1001)
[2/5] Verifying Environment Variables     [OK]
 ... All required Envs correct
[3/5] Checking Required Files             [OK]
 ... Verified 'secret.key' with correct key string.
[4/5] Checking Port Availability          [OK]
 ... Port 15034 is available.
[5/5] Verifying Log Permission            [OK]
 ... Log directory is writable: /var/log/agent-app
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
2026-05-13 06:21:43,592 [INFO] [SafetyGuard] Process priority lowered (nice=10).
2026-05-13 06:21:43,592 [INFO] Agent listening at port 15034
2026-05-13 06:21:43,592 [INFO] === Agent Started. Beginning resource cycle. ===
2026-05-13 06:21:43,592 [INFO] --- Step Info: Mode=UP, CPU Lv=1, Mem=0MB ---
2026-05-13 06:21:43,630 [INFO] [Memory] Increasing... (+25 MB) Total: 25 MB
2026-05-13 06:21:43,657 [INFO] [CPU] Level 1 workload completed. Duration: 0.03s
2026-05-13 06:21:44,658 [INFO] --- Step Info: Mode=UP, CPU Lv=2, Mem=25MB ---
2026-05-13 06:21:44,697 [INFO] [Memory] Increasing... (+25 MB) Total: 50 MB
2026-05-13 06:21:44,750 [INFO] [CPU] Level 2 workload completed. Duration: 0.05s
2026-05-13 06:21:45,753 [INFO] --- Step Info: Mode=UP, CPU Lv=3, Mem=50MB ---
2026-05-13 06:21:45,793 [INFO] [Memory] Increasing... (+25 MB) Total: 75 MB
2026-05-13 06:21:45,872 [INFO] [CPU] Level 3 workload completed. Duration: 0.08s


[붙여넣기] 0.0.0.0:15034 LISTEN 확인 결과
root@agent-lab:/# 
root@agent-lab:/# ss -ltnp | grep ':15034'
LISTEN 0      1            0.0.0.0:15034      0.0.0.0:*                                 
root@agent-lab:/# 


```

## 9. monitor.sh 실행 확인

`monitor.sh`는 컨테이너 내부 경로 `/home/agent-admin/agent-app/bin/monitor.sh`에 배치되어 있다.

실행 명령:

```bash
sudo -u agent-admin bash -lc 'source ~/.bashrc && "$AGENT_HOME/bin/monitor.sh"'
```

확인 내용:

- `agent-app` 프로세스 확인
- `15034` 포트 LISTEN 확인
- UFW 활성화 상태 확인
- CPU, 메모리, 디스크 사용률 수집
- 임계값 초과 시 `[WARNING]` 출력
- `/var/log/agent-app/monitor.log`에 한 줄 기록

증거 기록:

```text
[붙여넣기] monitor.sh 실행 결과
[붙여넣기] 프로세스 [OK] 결과
[붙여넣기] 포트 [OK] 결과
[붙여넣기] 자원 사용률 및 WARNING 결과

root@agent-lab:/# 
root@agent-lab:/# sudo -u agent-admin bash -lc 'source ~/.bashrc && "$AGENT_HOME/bin/monitor.sh"'
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app'... [OK] (PID: 220)
Checking port 15034... [OK]
Checking UFW firewall status... [OK]

[RESOURCE MONITORING]
CPU Usage : 1.7%
MEM Usage : 3.8%
DISK Used : 1%


[INFO] Log appended: /var/log/agent-app/monitor.log
root@agent-lab:/# 

root@agent-lab:/# exit
exit
keytest1591@c4r9s8 B1-1 % 
keytest1591@c4r9s8 B1-1 % 
keytest1591@c4r9s8 B1-1 % 
keytest1591@c4r9s8 B1-1 % docker compose exec --user agent-admin agent-lab bash
agent-admin@agent-lab:/$ 
agent-admin@agent-lab:/$ source ~/.bashrc
agent-admin@agent-lab:/$ 
agent-admin@agent-lab:/$ export MEM_THRESHOLD=1.2
agent-admin@agent-lab:/$ $AGENT_HOME/bin/monitor.sh
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app'... [OK] (PID: 220)
Checking port 15034... [OK]
Checking UFW firewall status... [OK]

[RESOURCE MONITORING]
CPU Usage : 2.7%
MEM Usage : 4.8%
DISK Used : 1%

[WARNING] MEM threshold exceeded (4.8% > 1.2%)

[INFO] Log appended: /var/log/agent-app/monitor.log
agent-admin@agent-lab:/$ 
agent-admin@agent-lab:/$ 



```

## 10. 로그 누적 확인

확인 명령:

```bash
tail -n 5 /var/log/agent-app/monitor.log
```

로그 형식:

```text
[YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK_USED:..%
```

증거 기록:

```text
[붙여넣기] /var/log/agent-app/monitor.log 최근 라인

root@agent-lab:/# 
root@agent-lab:/# tail -n 5 /var/log/agent-app/monitor.log
[2026-05-13 06:30:02] PID:13127 CPU:0.5% MEM:4.0% DISK_USED:1%
[2026-05-13 06:31:02] PID:13127 CPU:2.0% MEM:4.4% DISK_USED:1%
[2026-05-13 06:32:02] PID:13127 CPU:2.7% MEM:4.6% DISK_USED:1%
[2026-05-13 06:33:02] PID:13127 CPU:1.5% MEM:3.9% DISK_USED:1%
[2026-05-13 06:34:02] PID:13127 CPU:4.8% MEM:5.4% DISK_USED:1%

```

## 11. cron 매분 자동 실행 확인

컨테이너 내부 `agent-admin` crontab에 `monitor.sh`를 매분 실행하도록 등록했다.

확인 명령:

```bash
crontab -u agent-admin -l
tail -n 5 /var/log/agent-app/monitor.log
sleep 70
tail -n 5 /var/log/agent-app/monitor.log
```

증거 기록:

```text
[붙여넣기] agent-admin crontab 등록 결과

root@agent-lab:/# crontab -u agent-admin -l
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor.cron.out 2>&1

[붙여넣기] 1분 전 monitor.log 결과
[붙여넣기] 1분 후 monitor.log 증가 결과
root@agent-lab:/# tail -n 5 /var/log/agent-app/monitor.log
[2026-05-13 06:35:02] PID:13127 CPU:1.0% MEM:4.1% DISK_USED:1%
[2026-05-13 06:36:03] PID:13127 CPU:3.3% MEM:4.6% DISK_USED:1%
[2026-05-13 06:37:02] PID:13127 CPU:2.5% MEM:4.9% DISK_USED:1%
[2026-05-13 06:38:02] PID:13127 CPU:0.0% MEM:3.8% DISK_USED:1%
[2026-05-13 06:39:02] PID:13127 CPU:4.5% MEM:4.8% DISK_USED:1%
root@agent-lab:/# 

```

## 12. 제출 전 체크리스트

- [x] 컨테이너 `b1-1-agent-lab` 실행 확인
- [x] SSH 포트 `20022` 확인
- [x] Root 원격 접속 차단 확인
- [x] UFW 활성화 및 `20022/tcp`, `15034/tcp` 허용 확인
- [x] 계정 `agent-admin`, `agent-dev`, `agent-test` 확인
- [x] 그룹 `agent-common`, `agent-core` 확인
- [x] 디렉토리 권한 및 ACL 확인
- [x] 환경 변수 `AGENT_*` 확인
- [x] 키 파일 `t_secret.key` 확인
- [x] 앱 Boot Sequence 5단계 `[OK]` 확인
- [x] `Agent READY` 확인
- [x] 앱 `15034` LISTEN 확인
- [x] `monitor.sh` 실행 결과 확인
- [x] `monitor.log` 누적 기록 확인
- [x] cron 매분 실행 및 1분 후 로그 증가 확인


